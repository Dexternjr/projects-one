class MotivationalMessage {
    constructor() {
        this.message = "Work hard and be consistent";
    }

    printMessage() {
        console.log(this.message);
    }
}

// Create an instance of the MotivationalMessage class
const motivation = new MotivationalMessage();

// Print the motivational message
motivation.printMessage();
